const config = {
    serverConfig:{
        host: process.env.APP_HOST || 'https://61aaaf2834e347008fcc4139--relaxed-morse-4883ee.netlify.app/',
        //port: process.env.APP_PORT || 3000
    }
}

module.exports = config